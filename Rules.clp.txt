(deffacts SemNet (initial_fact))

(defrule bob (initial_fact)
	 => 
	(assert(ISA duck bird))
	(assert(ISA hawk bird))
	(assert(ISA hawk carnivore))
	(assert(ISA bat mammal))
	(assert(ISA bird oviparous))
	(assert(ISA dog canine))
	(assert(ISA dog omnivore))
	(assert(ISA wolf canine))
	(assert(ISA wolf carnivore))
	(assert(ISA fox canine))
	(assert(ISA fox omnivore))
	(assert(ISA lion feline))
	(assert(ISA cat feline))
	(assert(ISA lynx feline))
	(assert(ISA feline carnivore))
	(assert(ISA canine mammal))
	(assert(ISA feline mammal))
	(assert(ISA mammal animal))
	(assert(ISA bird animal))
	(assert(ISA chimp primate))
	(assert(ISA human primate))
	(assert(ISA primate mammal))
	(assert(ISA primate omnivore))
)
(defrule feature (initial_fact)
=>
	(assert(feature warm-blood mammal))
	(assert(feature sharp-teeth feline))
	(assert(feature sharp-teeth canine))
	(assert(feature have-non-sharp-canine primate))
	(assert(feature opposable-thumbs primate))
	(assert(feature have-feather bird))
	(assert(feature have-claws bird))
	(assert(feature eat-everything primate))
	(assert(feature fly bird))
	(assert(feature eat-only-meat carnivore))
	(assert(feature travel-by-walking mammal))
)

(defrule CanineMammals (ISA canine mammal) (ISA ?child canine) => (assert(ISA ?child mammal)))
(defrule FelineMammals (ISA feline mammal) (ISA ?child feline) => (assert(ISA ?child mammal)))
(defrule FelineCarnivore (ISA feline carnivore) (ISA ?child feline) => (assert(ISA ?child carnivore)))
(defrule primateOmnivores (ISA primate omnivore)(ISA ?child primate) => (assert(ISA ?child omnivore)))
(defrule BirdOviparous (ISA bird oviparous)(ISA ?child bird) =>(assert(ISA ?child oviparous)))
(defrule HawkCarnivore (ISA bird carnivore)(ISA ?child bird) => (assert(ISA ?child carnivore)))
(defrule CanineTeeth (ISA ?child canine) (feature sharp-teeth canine) => (assert(feature sharp-teeth ?child)))
(defrule FelineTeeth (ISA ?child feline) (feature sharp-teeth feline) => (assert(feature sharp-teeth ?child)))
(defrule MammalBlood (ISA ?child mammal) (feature warm-blood mammal) => (assert(feature warm-blood ?child)))
(defrule PrimateTeeth (ISA ?child primate) (feature have-non-sharp-canine primate) => (assert(feature have-non-sharp-canine ?child)))
(defrule OpposablePrimate (ISA ?child primate) (feature opposable-thumbs primate) => (assert(feature opposable-thumbs ?child)))
(defrule FeatherBird (ISA ?child bird) (feature have-feather bird) => (assert(feature have-feather ?child)))
(defrule clawsBird (ISA	?child bird) (feature have-claws bird) => (assert(feature have-claws ?child)))
(defrule Primateat (ISA ?child primate) (feature eat-everything primate) => (assert(feature eat-everything ?child)))
(defrule FlyBrid (ISA ?child bird) (feature fly bird) => (assert(feature fly ?child)))
(defrule MeatCar (ISA ?child carnivore) (feature eat-only-meat carnivore) => (assert(feature eat-only-meat ?child)))
(defrule WalkingMam (ISA ?child mammal) (feature travel-by-walking mammal)(test (neq ?child bat)) => (assert(feature travel-by-walking ?child)))

(defrule PrintDogInfo(declare (salience -10))(ISA dog ?type)=>(printout t "Dog is a " ?type crlf))
(defrule PrintCatInfo(declare (salience -9))(ISA cat ?type)=>(printout t "Cat is a " ?type crlf))
(defrule PrintHumanInfo(declare (salience -8))(ISA human ?type)=>(printout t "Human is a " ?type crlf))